services:
  home-control:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ~/.android:/root/.android:ro
    restart: unless-stopped
